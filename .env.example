DATABASE_URL=postgres://peek:peek@localhost:5432/peek
SESSION_SECRET=change-me-to-a-long-random-string
APP_URL=http://localhost:5173
PORT=3000
DOCKER_SOCKET_PATH=/var/run/docker.sock

# Optional — host system metrics when running Peek in Docker (requires matching volume mounts)
# HOST_PROC_ROOT=/host/proc
# HOST_SYS_ROOT=/host/sys
# HOST_ROOTFS=/rootfs
